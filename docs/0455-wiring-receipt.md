# 0455 wiring receipt — v6 candidate through the production-candidate boundary

**Date:** 2026-09-18
**Status:** COMPLETE for the repo-local boundary. Storage upload + `--finalize` deliberately NOT done (operator-gated, see §6).

## 1. What shipped

The 0455 runtime the app serves in dev (`pastpaperprep/src/data/production/igcse-economics-0455.json`)
was still the Sep-15 sealed artifact built from v5: slivered mark-scheme crops, 158 null
marks, 184 furniture summaries, 30 silently-lost rows, and a wrong 30-mark total served
on 34 MCQ rows (the `paper_maximum_mark` leak). The repaired artifacts existed but were
not wired.

Now wired, end to end, through the committed release architecture:

| Layer | Artifact | State |
|---|---|---|
| Segmentation | `data/segmentation/full-repaired/full-manifest.json` | 70 papers / 1,225 questions; 0 stray words, 0 slivers, 0 furniture (audits + determinism proven earlier) |
| Classification | `data/classification/full-selected-v6-row-restore/selected-candidate.json` | 1,220 rows = 1,189 refreshed v5 + 30 restored + 1 honest block; sha `9fae73bc…` |
| Projected runtime | `pastpaperprep/src/data/local-preview/igcse-economics-0455.json` | 1,219 rows via canonical writer + `ECON0455_CANDIDATE`; sha `38e74b66…` |
| Production candidate | `pastpaperprep/src/data/production/igcse-economics-0455.json` | self-sealed, `assetVerification: pending_storage_release`; candidate sha `69bfa6b0…`, runtime sha `5b66581b…` |
| Private index | `pastpaperprep/src/data/private-index/igcse-economics-0455.json` | 1,219 metadata-only rows, regenerated by the same generator |
| Release manifest | (dry-run only) | 3,018 unique assets, all resolving + hashing against the repaired lane; `pending_upload` |

## 2. The v6 build (bank repo)

`data/classification/full-selected-v6-row-restore/build_v6.py` — fail-closed, all gates
printed in `build-receipt-v6.json`:

- **v5 byte-identity:** every v5 row's labels/proofs/order unchanged; the in-build
  canonical proof reduces asset dicts to `(paper, role, question, page)` identity and
  refuses any non-asset drift. 1,189/1,189 pass.
- **30 restored rows:** Sep-10 blind A/B packets adjudicated against printed evidence —
  18 print-grounded rulings (including 4 rows both blind reviewers had marked
  `taxonomy_gap`; the registry's 6.3.x exchange-rate bullets own them). Receipt row
  entries record every ruling.
- **Marks:** printed QP brackets backfilled on 158 rows (window rule: label→next-label,
  exactly one bracket); the official-MS row-omission row `0455-2022-s-21-q4` rebuilt from
  QP print (a)–(d) = 20 with `official_mark_scheme_row_absent` markers.
- **MCQ gate:** every served MCQ mark = 1, and served rows per paper == printed key rows
  (y-position pairing of number+letter); 1,045 rows / 35 papers / 30 each — exact.
- **Determinism:** two independent builds byte-identical (`9fae73bc…` twice).

## 3. The app-side wiring (pastpaperprep, branch design/ui-polish-and-feedback)

New: `scripts/generate-igcse-economics-0455-runtime.mjs` (the 0654 generator's twin —
sha-pinned inputs, shape asserts, seal formulas recovered from the sealed release
artifacts, `pending_storage_release` honesty until the operator's storage step).
Changed: `scripts/igcse-release.mjs` (econ candidate sha + repaired-lane asset mapping;
the old packet-028 special case is subsumed — the repaired lane emits that page), its
test, `src/lib/igcse-runtime.ts` (counts `[1219, 70]` + seal pins), `src/lib/catalog.ts`
(1,189 → 1,219), `src/lib/private-bank-free-funnel.test.ts` (census 241 free 2021 rows;
funnel reads the unfinalized candidate directly, as 0654's already did; one stale
cache-control assertion aligned with the committed metadata-only index contract).
Replaced: `src/lib/igcse-economics-0455-local-preview.test.ts` (stale pre-release
architecture, 8 pre-existing failures) → `src/lib/igcse-economics-0455-release.test.ts`
(11 tests, green).

**Verification:** focused suites 47/47 + release-script 12/12 green; `tsc --noEmit` clean.

## 4. Runtime quality (what a student now sees in dev)

- marks distribution: `1 × 1045 (MCQ) · 20 × 140 · 30 × 34` — 0 nulls; the only 30s are
  the genuinely-30-mark Paper-2 q1 mega-source questions (printed totals).
- 0 empty `accessibleText`, 0 furniture-leading summaries/rows.
- All 3,018 asset references resolve to repaired-lane webp bytes (manifest dry-run).

## 5. Known honest limitations

- `0455-2021-s-22-q1` stays excluded (`unavailable_stimulus` blocker, counted).
- Whole-bank semantic approval remains `not_claimed`; the blind QA pattern used for the
  biology lane has not been run against the 30 restored rows' labels (the A/B agreement
  + printed adjudication is the evidence; a QA pass would strengthen it).
- 0654/0620/0610 repairs and extension-lane assemblies are separate threads' work.

## 6. Release steps (operator-gated, deliberately not executed)

1. Review + merge the branch (nothing merged, nothing pushed by this thread).
2. `ECON0455_SOURCE_ROOT=… node scripts/generate-igcse-release.mjs igcse-economics-0455`
   → writes `data/storage/igcse-economics-0455.manifest.json` (dry-run verified above).
3. Storage upload + full GET SHA-256 readback → `…receipt.json` (resumable, create-only).
4. `node scripts/generate-igcse-release.mjs --finalize igcse-economics-0455` → flips the
   runtime to `production` + `verified_readback` seals; then env gates
   (`PASTPAPERPREP_ENABLE_IGCSE_RELEASE_BANKS` + `…ASSETS_VERIFIED`) expose it.
5. Live QA per the release checklist.
